1.1. Необходимо восстановить данные в полном объёме за предыдущий день.

Вариант	Скорость	Место	Вердикт
Полный	4*	1*	Быстро, но жрет много места
Инкрементный	2*	5*	Экономно, но трудно восстанавливать
Дифференциальный	4*	3*	Лучший выбор – быстро и умеренно по месту

1.2. Необходимо восстановить данные за время до предполагаемой неисправности. 

Всем трём нужны журналы изменений (логи), чтобы остановиться на нужном времени.
Вариант	Сложность	Риск	Вердикт
Полный	Средняя	Низкий	Как по мне лучший вариант полный бэкап + журналы
Инкрементный	Высокая (много цепочек)	Высокий (сломается один - всё)	Не наш вариант
Дифференциальный	Средняя	Средний	Середнячок

1.3. Моментальное переключение при поломке
Мне кажется, ни один бэкап не подходит, они все требуют времени на восстановление.
Нужна репликация - второй сервер-копия, который будет подхватывать работу.

_________________________________________________________________________________

Задание 2. PostgreSQL
2.1. Команды резервирования и восстановления
Резервирование (создание бэкапа) — утилита pg_dump
Проще всего сделать дамп в виде обычного SQL-файла:
```bash
pg_dump -U postgres -d имя_базы > backup.sql```

Где -U — пользователь, -d — название базы данных, > — сохраняем результат в файл.
Для больших баз удобнее использовать кастомный формат (сжатый и гибкий):
```bash
pg_dump -U postgres -Fc -f backup.dump имя_базы```
-Fc — формат custom, -f — имя выходного файла.

Восстановление — утилита pg_restore

Если бэкап в SQL-формате, восстанавливаем через psql:

```bash
psql -U postgres -d новая_база < backup.sql
Если бэкап в кастомном формате .dump:```

```bash
pg_restore -U postgres -d новая_база -c backup.dump```
Флаг -c очищает базу перед восстановлением.

2.1.* Автоматизация резервного копирования
Да, автоматизировать можно — и это стандартная практика.

Самый простой способ — добавить скрипт в Cron
Создаёшь bash-скрипт и добавляешь его в планировщик:
bash
#!/bin/bash
# Скрипт: /usr/local/bin/pg_backup.sh
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
```pg_dump -U postgres -Fc -f /backups/db_${TIMESTAMP}.dump имя_базы```

# Удаляем бэкапы старше 7 дней
```find /backups -name "*.dump" -mtime +7 -delete```
Добавляешь в Cron (ежедневно в 2 часа ночи):
bash
```0 2 * * * /usr/local/bin/pg_backup.sh >> /var/log/pg_backup.log 2>&1```
Есть и готовые решения:
autopostgresqlbackup — специальный инструмент из репозиториев Debian, который сам настраивает ежедневные, еженедельные и ежемесячные бэкапы, удаляет старые копии и может отправлять отчёты по почте.
Docker-контейнеры — готовые образы с автоматическим бэкапом и отправкой в Telegram или облако.
Главное правило автоматизации: бэкап должен работать без участия человека, а про успех или ошибку лучше узнавать из логов или уведомлений.
